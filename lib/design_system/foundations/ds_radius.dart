class DSRadius {
  static const double xs = 4.0;    // Мелкие элементы (тэги)
  static const double s = 8.0;    // Карточки в расписании, кнопки
  static const double m = 16.0;   // Обложки альбомов (Track Card)
  static const double l = 20.0;  // Элементы плеера или большие панели
  static const double xl = 24.0;  // Карточки/панели (из дизайна)
  static const double xxl = 28.0; // Элементы плеера или большие панели

  // Круглые элементы (аватарки, кнопки Play)
  static const double circular = 100.0;
}