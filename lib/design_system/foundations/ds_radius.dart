class DSRadius {
  static const double s = 4.0;    // Мелкие элементы (тэги)
  static const double m = 8.0;    // Карточки в расписании, кнопки
  static const double l = 10.0;   // Карты в музыке
  static const double xl = 12.0;   // Обложки альбомов (Track Card)
  static const double xxl = 24.0;  // Элементы плеера или большие панели
  
  // Круглые элементы (аватарки, кнопки Play)
  static const double circular = 100.0;
}